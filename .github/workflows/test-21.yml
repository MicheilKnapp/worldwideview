name: Test 21 If Not Cancelled
on:
  push:
    branches: [test/gha-binsearch-1]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello"
      - if: ${{ !cancelled() }}
        run: echo "not cancelled"
