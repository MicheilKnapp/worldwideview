name: Test 25 Secret Bare
on:
  push:
    branches: [test/gha-binsearch-1]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello"
      - if: ${{ secrets.MY_SECRET }}
        run: echo "secret exists"
